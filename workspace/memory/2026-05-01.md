# Memory 2026-05-01

## System Configuration Changes

### Privileged Mode (Elevated) Configuration
- Modified `openclaw.json` to configure `tools.elevated` settings
- Set global `tools.elevated.enabled = true`
- Disabled elevated mode for all non-main agents by adding `tools.elevated.enabled = false` to each agent config
- Only `agent:main:main` retains access to elevated/privileged tools
- Gateway restarted successfully to apply changes

### DeepSeek Configuration Discussion
- User asked about DeepSeek V4 support and configuration issues
- Current deepseek provider uses `api: openai-completions` (not openai-chat)
- Both deepseek-v4-flash and deepseek-v4-pro have `reasoning: false`
- Attempted to modify configuration but provided unverified changes that were rejected by Gateway (`reload-invalid-config`)
- Gateway auto-restored configuration from last-known-good backup
- Lesson: must verify configuration schema before making changes to openclaw.json

## User Requests
- Asked to check which sessions have elevated mode enabled
- Asked about DeepSeek V4 configuration problems
- Requested switch to K2.6 model (moonshot/kimi-k2.6)

## Session State
- Model switched from `kimicode/kimi-for-coding` to `moonshot/kimi-k2.6`
- Current session: agent:main:main with elevated mode enabled
