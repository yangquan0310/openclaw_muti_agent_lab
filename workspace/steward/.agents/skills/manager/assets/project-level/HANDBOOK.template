# {project_name} 操作手册

> 版本：1.0.0

---

## 项目入门

任何 Agent 进入本项目后，必须按以下顺序执行：

```
1. 阅读 README.md
   └── 了解项目元信息（名称、类型、创建时间）
   └── 了解项目目的（为什么创建、解决什么问题、预期产出）
   
2. 阅读 HANDBOOK.md（本文件）
   └── 了解协作规范（大管家调度、其他代理自主执行）
   └── 了解文件管理规则（每轮只生成一份文件、Git 版本控制）
   └── 了解文件归档规范（产出物存放目录）
   └── 了解保护文件清单（禁止移动/修改的文件）
   
3. 打开 metadata.json
   └── 了解项目元数据（类型、版本、状态）
   
4. 打开 {项目路径}/.agents/agents/
   └── 查看自己的角色文件
   └── 明确自己的职责边界
   
5. 打开 TODO.md
   └── 了解当前任务状态
   └── 确认自己负责的任务项
```

---

## 协作规范

**大管家负责协调+追踪任务（不替子代理执行）。**
- 大管家根据老板指令更新 TODO.md，经确认后使用 **manager 技能** 派发任务
- 大管家追踪各代理进度，在 TODO.md 中同步更新状态
- **大管家不规定子代理具体怎么执行**——只通过 TODO 的"目标/约束/输入/产出"规定 WHAT，HOW 由子代理自决

**其他代理自主执行并反馈。**
- 其他代理接收任务后自行决定具体执行方式，不得被干涉
- 任务完成后 → 在群聊艾特大管家汇报（❌ 禁止私聊）
- 汇报内容必须包含：任务名称 + 产出文件

**子任务必须在 TODO.md 中发布。**
- 任何需要其他代理协作的子任务，必须先在 TODO.md 中创建任务项
- 任务项 7 字段（老板 2026-06-03 定型）：任务ID、任务描述、负责人、目标、约束、输入、产出、状态（详见 task-flow-guide.md v2.3）
- 4 必填内容字段：目标、约束、输入、产出（子代理**必须遵守的内容**）
- 代理收到任务通知后，应先查看 TODO.md 的"目标/约束/输入/产出"

**认领（claim）协议。**
- 代理收到大管家 IM 群通知后，用 workboard_claim 插件工具认领该 card
- 认领后在群里回复"已认领 card={{card_short}}"
- 大管家**只在看到群里认领后**才调 `manager workboard start` 触发 run

**文件管理规则**
- 每个轮次只生成一份文件，直接在文件上修改
- 不创建分支版本（如v2.1、v4.1等）
- 所有修改通过 Git 版本控制
- 如需回退，使用 `git checkout` 或 `git revert`，不依赖文件副本

---

## 保护文件

以下文件禁止移动/修改：

| 文件 | 说明 |
|------|------|
| README.md | 项目总览 |
| HANDBOOK.md | 本文件 |
| TODO.md | 进度看板 |
| metadata.json | 项目元数据 |
| .agentignore | 可见性控制 |

---

## {custom_section_title}

{custom_section_content}

---

*最后更新：{created_at}*
*维护者：steward*
