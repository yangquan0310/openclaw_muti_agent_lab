# 2026-05-29 工作日志

---

## 博士论文排版

### 当前状态
- pandoc + xelatex 排版流程
- citeproc: false + natbib 方案（暂时）
- 所有 `[@XXX]` 已转为 `\citep{XXX}`

### citeproc: true 测试失败
- 使用 `input-files` 多文件输入时，citeproc 报错 "No citation element present"
- 原因：Pandoc 拼接多文件时 citeproc 解析问题
- 解决方案：需要用 `--file-scope` CLI 参数，但无法写入 pandoc.yaml

### 待验证方案
- 方案A：用 `--file-scope` CLI 参数测试
- 方案B：单文件 vs 多文件的 citeproc 行为差异

### 发现
- AI-Agent科普文章用 `citeproc: false` + `input-files` 成功
- 博士论文之前 citeproc 可以，现在不行——可能是文件数量变化

