# REM Sleep

### Reflections
- No strong patterns surfaced.

### Possible Lasting Truths
- citeproc: true 测试失败: 使用 `input-files` 多文件输入时，citeproc 报错 "No citation element present"; 原因：Pandoc 拼接多文件时 citeproc 解析问题; 解决方案：需要用 `--file-scope` CLI 参数，但无法写入 pandoc.yaml [confidence=0.58 evidence=memory/2026-05-29.md:13-15]
- LaTeX必须用.tex文件的功能: `\usepackage{}`; `\setlength{}`; `\renewenvironment{}` [confidence=0.58 evidence=memory/2026-05-28.md:124-126]
- Pandoc YAML配置要点: `csl: apa.csl`：引用样式; `citeproc: false`：使用natbib/pandoc-citeproc处理引用; `include-in-header: header.tex`：必须用文件路径; `variables: {}`：LaTeX变量（字体、页面尺寸等） [confidence=0.58 evidence=memory/2026-05-28.md:118-121]
