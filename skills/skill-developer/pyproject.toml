[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "skill-developer"
version = "5.1.0"
description = "OpenClaw 技能开发元技能"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["scripts*"]
