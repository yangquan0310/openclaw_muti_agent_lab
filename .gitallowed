# Git Secrets Allowed Patterns
# These patterns are explicitly allowed to reduce false positives

# Environment variable references (proper syntax)
\\$\\{[A-Z_]+\\}

# Masked/placeholder values
\\*\\*\\*

# Non-secret field names
maxTokens
